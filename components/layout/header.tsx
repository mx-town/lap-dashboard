"use client"

// This component is now deprecated - header logic moved inline to MainLayout
// Keeping file for backwards compatibility but it's no longer used
export function Header() {
  return null
}
